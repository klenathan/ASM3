# Uncomment either static access keys or AWS_PROFILE. Do not use both approaches.
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_SESSION_TOKEN=""
# AWS_PROFILE=""

# Keep this aligned with aws_region in infras/terraform.tfvars.
AWS_REGION="us-east-1"
STATE_BUCKET=""

# Embedded into the static frontend bundle by make deploy-frontend.
# Use a URL/origin-restricted Mapbox public token (pk.*).
# VITE_MAPBOX_PUBLIC_TOKEN=""

# Used only when exercising the content-analysis Lambda locally. Production
# stores this value in AWS Secrets Manager and passes its ARN to Lambda.
# OPENROUTER_API_KEY=""
# OPENROUTER_MODEL="deepseek/<approved-model-slug>"

# Content-analysis (review) pipeline.
# CONTENT_ANALYSIS_MODE=off           # off | shadow | enforce (default: off)
# CONTENT_ANALYSIS_LAMBDA_FUNCTION="rmit-society-content-analysis"
# CONTENT_ANALYSIS_LAMBDA_QUALIFIER="prod"
# CONTENT_ANALYSIS_POLICY_VERSION="v2"
# CONTENT_ANALYSIS_PROMPT_VERSION="v1"
# CONTENT_ANALYSIS_AUTO_REMOVE_CONFIDENCE=0.90  # [0,1]; threshold for auto-removing high-confident high-severity findings (default: 0.90; no effect in off/shadow mode)
# CONTENT_ANALYSIS_QUEUE_URL="https://sqs.us-east-1.amazonaws.com/123456789012/rmit-society-demo-content-analysis-reanalysis"
# ANALYSIS_TIMEOUT_MS=55000       # capped below the SQS visibility deadline
# ANALYSIS_STALE_AFTER_MS=900000    # 15 min; pending runs older than this are auto-retried
# ANALYSIS_RETRY_INTERVAL_MS=60000  # scheduler tick between stale-run retry passes
# ANALYSIS_MAX_RETRIES=3             # maximum Lambda invocations per analysis run
# ANALYSIS_MAX_IMAGES=4
# ANALYSIS_MAX_IMAGE_BYTES=10485760   # 10 MB, matches FE upload / backend media
# ANALYSIS_MAX_TOTAL_IMAGE_BYTES=41943040   # 4 x 10 MB
