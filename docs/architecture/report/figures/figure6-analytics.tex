\documentclass[tikz,border=8pt]{standalone}
\usepackage{lmodern}\usetikzlibrary{arrows.meta,positioning,fit,backgrounds}
\definecolor{optional}{HTML}{FFF1DE}\tikzset{box/.style={draw,rounded corners,fill=optional,minimum width=28mm,minimum height=11mm,align=center,font=\sffamily\scriptsize},client/.style={box,fill=black!5},edge/.style={-{Latex[length=1.6mm]},thick,font=\sffamily\tiny,align=center},async/.style={edge,dashed}}
\begin{document}\begin{tikzpicture}[node distance=8mm and 9mm]
\node[client] (admin) {System Admin\\Admin Center};\node[box,right=of admin] (ecs) {1--2. ECS backend\\durable refresh run};\node[box,right=of ecs] (rds) {RDS PostgreSQL\\run + metric rows};\node[box,below=of ecs] (sfn) {3. Step Functions\\range-scoped execution};\node[box,left=of sfn] (event) {EventBridge\\scheduled trigger};\node[box,right=of sfn] (glue) {4--5. Glue + Catalog\\Parquet export/tables};\node[box,right=of glue] (s3) {Private S3\\analytics export};\node[box,below=of glue] (athena) {6. Athena\\parallel SQL queries};\node[box,right=of athena] (lambda) {7. Workflow Lambda\\validate/persist results};
\draw[edge] (admin) -- node[above]{1. bounded UTC range} (ecs);\draw[edge] (ecs) -- node[above]{2. create run} (rds);\draw[async] (ecs) -- node[right]{3. start execution} (sfn);\draw[async] (event) -- node[above]{scheduler endpoint} (ecs);\draw[async] (sfn) -- node[above]{4. export} (glue);\draw[async] (glue) -- node[above]{Parquet} (s3);\draw[async] (sfn) |- node[pos=.25,left]{6. activity/current/reconciliation} (athena.west);\draw[async] (athena) -- (lambda);\draw[async] (lambda) |- node[pos=.28,right]{7. metric rows} (rds.south);\draw[edge] (rds.west) to[bend right=28] node[below]{8. metrics API/table/chart} (admin.south);
\begin{scope}[on background layer]\node[draw,dashed,rounded corners,fit=(ecs)(rds)(sfn)(event)(glue)(s3)(athena)(lambda),inner sep=7mm,label={[font=\sffamily\small]above:Feature-gated analytics pipeline --- AWS Cloud us-east-1}] {};\end{scope}
\node[font=\sffamily\footnotesize,align=center,text width=154mm,below=25mm of athena] {Inclusive-start/exclusive-end UTC range. Step Functions owns waits, retries, timeouts, and fan-in; ECS does not poll Glue or Athena. Action-event actors are HMAC pseudonyms.};
\end{tikzpicture}\end{document}
