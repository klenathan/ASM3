"""Shared kernel for the RMIT Society backend.

Cross-cutting domain types and constants that multiple bounded contexts
depend on. Contexts may depend on this package; it must never depend on a
bounded context.
"""
